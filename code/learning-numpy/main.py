import numpy as np

array = np.array([[1., 0., 0.],
                    [0., 1., 2.]])

print("Array dimension is " + str(array.ndim))

print("Array shape is " + str(array.shape))

print("Array size is " + str(array.size))
